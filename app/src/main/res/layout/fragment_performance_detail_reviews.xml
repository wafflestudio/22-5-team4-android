<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <TextView
        android:id="@+id/reviewInformText"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="공연장의 열기를 그대로!\n생생하고 실감나는 후기를 남겨주세요."
        android:gravity="center_horizontal"
        android:layout_marginVertical="12dp"/>
    <Button
        android:id="@+id/NavWriteReviewButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="관람후기 작성"
        android:background="@drawable/bg_nav_write_review_button"
        app:layout_constraintTop_toBottomOf="@id/reviewInformText"
        android:layout_marginTop="12dp"
        android:layout_marginHorizontal="12dp"/>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/reviewRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:orientation="vertical"
        android:nestedScrollingEnabled="true"
        android:layout_marginTop="128dp"/>
</androidx.constraintlayout.widget.ConstraintLayout>